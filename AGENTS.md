# 项目约定

本仓库是一个 Vibe Coding 玩具项目集合，用于存放独立的小项目和实验。

## 目录结构

- 每个项目放在根目录下的独立子目录中，子目录名即为项目名。
- 新项目使用简洁、能表达用途的目录名，推荐小写英文与连字符，例如 `workout-timer`。
- 项目的源码、资源、配置、测试和使用说明均放在对应项目目录内。
- 项目专属的开发和验证约定写在该子目录的 `AGENTS.md` 中。

## 新增项目

1. 确定项目名，并创建同名子目录。
2. 在该目录内实现项目，提供必要的使用或启动说明。
3. 更新根目录 `README.md` 的项目列表，包含项目名和简短的项目简介，并使用相对链接指向项目目录或项目 README。
4. 执行与改动相适应的验证，确认 README 中的项目名与目录名一致、链接有效。

根目录 README 的项目条目示例：

```markdown
- [workout-timer](workout-timer/README.md)：运动计时器，支持设置开始等待、运动时间、运动组数和组间休息。
```

## 修改已有项目

- 将改动限定在目标项目及必要的根目录文档中，避免改动无关项目。
- 项目名称或功能简介发生变化时，同步更新根目录 `README.md`。
- 优先采用满足需求的简单实现，保持每个项目可独立使用。
